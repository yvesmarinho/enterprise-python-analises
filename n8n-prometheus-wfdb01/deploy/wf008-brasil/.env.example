# Variáveis de Ambiente - Servidor wf008 (Brasil)
# Copiar para .env e ajustar valores reais

# Docker User (non-root security)
# Obter com: id -u docker_user && id -g docker_user no servidor wf008
DOCKER_UID=1500
DOCKER_GID=985

# API Keys (mesma chave usada em wf001)
COLLECTOR_API_KEY=SUBSTITUA_POR_CHAVE_SEGURA_PRODUCAO

# Environment
LOG_LEVEL=INFO
ENVIRONMENT=production
