apiVersion: 1

datasources:
  - name: VictoriaMetrics
    type: prometheus
    access: proxy
    url: http://victoria-metrics:8428
    uid: prometheus # UID explícito para estabilidade
    isDefault: true
    editable: false
    jsonData:
      httpMethod: POST
      timeInterval: 30s
