# Copiar para .env e ajustar valores

# API Keys
COLLECTOR_API_KEY=dev-secret-key-12345
N8N_API_KEY=

# Database (já configurado no compose, mas pode sobrescrever)
POSTGRES_PASSWORD=devpass123
MYSQL_ROOT_PASSWORD=devpass123

# Monitoring
GRAFANA_ADMIN_PASSWORD=admin

# Development
LOG_LEVEL=DEBUG
ENVIRONMENT=development

# Docker User (non-root security)
# Obter UID/GID em cada servidor com: id -u docker_user && id -g docker_user
DOCKER_UID=1500
DOCKER_GID=985
